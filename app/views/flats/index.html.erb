<div class="container">
  <div class="col-12">
    <h1>Simple airbnb</h1>
  </div>
</div>
  <ul>
    <% @flats.each do |flat| %>
      <li>
        <div class="card">
          <h3><%= flat.name %></h3>
          <p><%= flat.description %></p>
          <p><%= flat.number_of_guests %> $<%= flat.price_per_night  %></p>
        </div>
        <%= link_to "More info", flat_path(flat)%>
      </li>
      <% end %>
  </ul>
